## Copy this file to config.yml and change the values to match your setup.
---
:gitlab_url: https://gitlab.example.com

# Found in My Profile -> Account -> Private token
:private_token: super_secret_key_from_gitlab

# For listing projects/snippets/etc, how many results would you like to see at once.
# Note: You will be able to override this with a flag at runtime.  This will just be a
# default. Value must be between 1 and 100. GitLab API imposes a 100 result limit.
:results_per_page: 20
